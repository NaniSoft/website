node_modules
.next
out
dist
.DS_Store
*.log
.env*
coverage
next-env.d.ts
tsconfig.tsbuildinfo

# Local worktrees
.worktrees/

# Scratch working notes (plans, specs, prototypes) — never tracked
.scratch/

# Tooling symlinks and parent-tool state (not part of this project)
.agents/
.claude/
skills-lock.json
.playwright-mcp/

# Next 16 auto-generates these AI-agent rule files on `next dev`; regenerate as
# needed rather than tracking generated copies.
AGENTS.md
CLAUDE.md

# OpenNext (Cloudflare Workers) build output + wrangler local state
.open-next/
.wrangler/

# Playwright output (apps/playground)
test-results/
playwright-report/

# Impeccable design-hook session state (the design.json sidecar IS tracked)
.impeccable/hook.cache.json
