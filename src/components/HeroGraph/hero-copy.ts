// Fixed homepage hero copy. The headline + highlighted path share the
// blue -> purple -> pink accent gradient, so the words and the graph read as
// one idea. Copy is locked to the platform positioning (no fabricated metrics).

export const HERO_COPY = {
  headline: 'See Every Path Into Your Systems Before Someone Else Does',
  flagship: 'Atlas, Bedrock, Keystone',
  subtext:
    'Nanisoft maps every route through your systems — ingress, identity, traversal, data — so the path an attacker would take is visible before they take it.',
  cta: 'Explore the Platform',
} as const
