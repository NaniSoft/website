# 10 — Lakehouse state + playbook engine + reactive spine

**What to build:** The simulator's heart — shared in-browser lakehouse state that evolves under a declarative playbook engine, with the spine reacting to each step and an inspector showing state after every step. The flagship playbook (Sensitive Product View Audit) runs end-to-end on the spine.

**Blocked by:** 09 (Playground spine rendered from the model).

**Status:** done (merged `734f1bc`, main; verified 2026-08-22). Plan: `docs/superpowers/plans/2026-08-22-lakehouse-state-playbook-engine.md`; design: `docs/superpowers/specs/2026-08-22-lakehouse-state-playbook-engine-design.md`.

- [x] Zustand store holds Bronze/Silver/Gold (`graph_nodes`/`graph_edges`) + SchemaRegistry + audit_log; session-only (resets on reload) + export/import + reset-to-seed — `apps/playground/app/_store/usePlayground.ts`
- [x] Declarative playbook engine: a use-case = ordered phase-steps, each with actor node + active edge + narrative + state mutate fn; flagship = 22-step Sensitive Product View Audit mirrored from `resources/TrueAccess_Schema_to_Visualization_Sequence.mermaid` — pure core `packages/architecture/src/{playground-state.ts,playbook.ts}` + tests
- [x] Controls: auto-run + single-step + reset; pace ~1.1s/step (tunable) — `apps/playground/app/_controls/Controls.tsx` (`STEP_PACE_MS=1100`)
- [x] Spine reacts: each step lights its actor node jade + animates its active edge; done edges turn teal; phase band tracks the active phase — controlled React Flow in `_spine/Spine.tsx` reading `deriveStatus(STEPS, cursor)`; 09's hardcoded `['anchor','conveyor','openbao']` folded into model-derived `PLATFORM_COMPONENTS` (carry-forward cleared)
- [x] Inspector surfaces the lakehouse state after every step — `apps/playground/app/_inspector/Inspector.tsx` (Bronze/Silver/Gold/Schema/Audit tabs + counts)
- [x] Tests assert steps 1→10 produce the teaching state (Bronze 10 rows, Silver 2 tables, Gold 5 nodes/4 edges, SchemaRegistry `Product/Sensitive:bool`, anomaly present); step 14 audit-log write; step 17 sets the finding (j.harper/P-1042/Payroll-NG/sensitive/no backing membership) and marks Gold edges `anomalous`/`ok`; step 22 run completes and stops; export → reset → import roundtrips preserving the finding — `packages/architecture/tests/{playground-state.test.ts,playbook.test.ts}` (20 new tests; 48/48 total green). Playwright no-vision verify: 22-step run → `{cursor:22, goldNodes:5, goldEdges:4, audit:3, findingUser:'j.harper'}`; Gold-tab anomaly; export→reset→import roundtrip `{beforeReset:0, afterImport:22, ok:true}`; human visual confirmed.