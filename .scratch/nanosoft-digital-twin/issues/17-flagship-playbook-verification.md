# 17 — Flagship playbook end-to-end verification

**What to build:** The full guided Sensitive Product View Audit demonstrably works across all six tools — the teaching spine's three-read-lens (Trino=row / Compass=edges / Superset=chart) and three-write-surface (DataGerry=author / Airflow=ingest / Atlas=govern) contrast is visible, and the twin is queryable and visualizable end to end.

**Blocked by:** 11 (Tool-overlay framework + DataGerry), 12 (Airflow), 13 (Trino), 14 (Atlas+OPA), 15 (Compass), 16 (Superset).

**Status:** done — merged to main (fast-forward at `34a5b2f`; coordinator-integrated & verified 2026-08-23). Coordinator sign-off recorded for the `deriveStatus` pure-core change (accepted: criterion 1 requires every beckoned tool node visited; single runtime consumer; red→green tested). One integration fix on main (`9e200c6`): a duplicated `ExportedState` declaration in `e2e/helpers.ts` (introduced by a late refactor after the isolated build ran) broke `next build` typecheck post-merge (TS2374); dup dropped, full battery re-verified green. Criterion 3 was pre-proven by unit tests; this close-out added committed coverage for the rest: a full-run export/import roundtrip unit test (criterion 4, `packages/architecture/tests/playground-state.test.ts`) and the tree's first Playwright suite (`apps/playground/playwright.config.ts`, app-level + `e2e/spec-a|b|c-*.spec.ts`; no-vision: roles/DOM/SVG attributes + dev-only `window.__playground` store state, never screenshots; dev-server webServer on :3001). Final counts: architecture vitest **124/124**, playground e2e **10/10** (2 smoke + A/B/C), `pnpm --filter playground build` exit 0. The e2e specs caught **three real product bugs, all fixed minimally**: (1) Controls Run/Pause mixed `border` shorthand + `borderColor` longhand across rerenders → React console.error at every run end; (2) `deriveStatus` never marked beckoned tool nodes visited — steps 5–7 act as `airbyte` while step 7 beckons `trailhead`, so the Trailhead chip alone stayed idle through the whole run (applied steps now add their `openTool` to `doneNodeIds`; unit-tested — **pure-core semantic change, flagged for coordinator sign-off**); (3) React Flow set `pointer-events: none` on every spine node wrapper (no node selectable/draggable/interactive at the RF level), making **all six tool chips unclickable by pointer** — overlays were only ever reachable programmatically; fixed via an `onNodeClick` handler in `Spine.tsx`. Compass drill-into and Superset filter remain deliberate UI-local read-lens exceptions per SPEC §4.8 (spec B asserts they leave the store byte-identical); the Superset boot-empty-Gold carry-forward is untouched (SPEC §6).

- [x] A complete auto-run of the 22-step flagship advances the spine through all six tools without errors, ending stopped at step 22
- [x] Single-step mode exposes each tool's one canonical action; performing each mutates/reads the same state auto-run does (one code path holds across all six)
- [x] The three read lenses show the same finding (`j.harper / P-1042`) differently; the three write surfaces each write their target (SchemaRegistry / Bronze / audit_log) — proven by existing green unit tests (122/122)
- [x] export → reset → import roundtrips a complete run preserving the finding
- [x] An inspector shows the lakehouse state evolve across the full run
