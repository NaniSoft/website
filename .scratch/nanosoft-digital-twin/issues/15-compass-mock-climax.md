# 15 — Compass mock (climax)

**What to build:** The most-faithful mock — the product's own differentiated value — showing the finding as edges in a traversal graph, auto-opening at the finding step as the one climax, with drill-into-node for the "why".

**Blocked by:** 10 (Lakehouse state + playbook engine + reactive spine), 11 (Tool-overlay framework + DataGerry mock).

**Status:** done — merged to main (`6051f86`; verified 2026-08-22)

- [x] Traversal graph shows the finding as edges: anomalous `viewed` edge (`j.harper →viewed→ P-1042`) jade, the missing `memberof` edge as a dashed gap, backed access teal, + narrative steps
- [x] Climax: auto-opens at the finding step (the one auto-open mid-run)
- [x] Canonical action: drill into a node — click `j.harper` → "viewed P-1042 (sensitive: true), no backing group membership"; click `P-1042` → "sensitive: true, Payroll-NG"; graph exploration = Compass's differentiated value
- [x] Reads the Atlas response (path + narrative) + Gold state; writes nothing
- [x] Most faithful of the six (Compass is the product — structured-echo fidelity doesn't apply); node-within-node containment deferred (carry-forward, not this ticket)